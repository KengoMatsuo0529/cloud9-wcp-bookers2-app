<!DOCTYPE html>
<html>
  <head>
    <title>BOOKER2</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
          <a class="navbar-brand" href="/">
            <span>Bookers</span>
          </a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
             <% if user_signed_in? %>
          <li>
           <a class="fas fa-home nav-link texr-light"><%= link_to "Home", user_path(current_user.id) %></a>
          </li>
          <li>
            <a class="fas fa-user nav-link texr-light"><%= link_to "Users", users_path %>
          </li>
          <li>
            <a class="fas fa-book-open nav-link texr-light"><%= link_to "Books", books_path %>
          </li>
          <li>
            <a class="fas fa-sign-out-alt nav-link texr-light"><%= link_to "logout", destroy_user_session_path, method: :delete %>
          </li>
        <% else %>
          <li>
            <a class="fas fa-home nav-link texr-light"><%= link_to "Home", root_path %></a>
          </li>
          <li>
            <a class="fas fa-link nav-link text-light"><%= link_to "About", home_about_path %></a>
          </li>
          <li>
            <a class="fas fa-user-plus nav-link texr-light"><%= link_to "sign up", new_user_registration_path %></a>
          </li>
          <li>
            <a class="fas fa-sign-in-alt nav-link texr-light"><%= link_to "login", new_user_session_path %></a>
          </li>
        <% end %>
          </ul>
        </div>
        </div>
      </nav>
    </header>
  <main class="mb-auto">
    <div class="container">
      <%= yield %>
    </div>
  </main>
  <footer class="bg-white text-black pt-5 pb-4">
      <div class="container">
        <div class="row no-gutters">
          <div class="col-md-4">
            <p>CopyRight Infratop.inc</p>
          </div>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
