<% if @book.errors.any? %>
  <%= @book.errors.count %>error prohibited this obj from being saved:
  <% @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>

<div class="row">

<%= render 'books/list', user: @user , book: @book %>

<div class="col-md-8 offset-md-1">
<h2>Books</h2>
<table class="table table-hover table-inverse">
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
    </tr>
  </thead>
   
  <tbody>
    <% @books.each do |book| %>
    <tr>
      <td>
        <img class="attachment user profile_image fallback"><%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no-img.png", size:"40x40" %></img>
      </td>
      <td><%= link_to book_path(book.id) do %>
      <p><%= book.title %></p></td>
        <% end %>    
      <td><p><%= book.body %></p></td>
    </tr>
    <% end %>
  </tbody>
</table>
  
</div>

</div>
