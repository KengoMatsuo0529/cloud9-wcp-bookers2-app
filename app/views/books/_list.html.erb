<h1>User Info</h1>
    <p>user画像 <%= attachment_image_tag user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %></p>
    <p>name <%= user.name %></p>
    <p>introduction <%= user.introduction %></p>
</div>

<%= link_to "edit", edit_user_path(@user) %>

<h1>New Book</h1>
  <%= form_with model:@book, local:true do |f| %>
    <h4>Title</h4>
  <%= f.text_field :title %>
    <h4>Opinion</h4>
  <%= f.text_field :body %>
  <%= f.submit "Create Book" %>
  <% end %>